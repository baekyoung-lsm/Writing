# Claude 작업 지침

## 1. 기본 원칙

사용자에게 작업 과정을 설명하는 것보다 실제 작업을 수행하는 것을 우선한다.

작업 시작 전에 관련 MD 문서와 기존 구현을 확인하고, 기존 구조를 최대한 존중하면서 필요한 부분만 수정한다.

사용자가 명시하지 않은 대규모 구조 변경, 라이브러리 교체, 파일 이동, 기능 삭제는 하지 않는다.

## 2. 문서 우선 확인

작업을 시작할 때 관련 MD 문서가 존재하면 먼저 확인한다.

우선순위는 다음과 같다.

1. CLAUDE.md
2. 현재 작업과 직접적으로 관련된 MD 문서
3. README.md
4. 관련 코드
5. 기타 파일

관련 MD 문서에 이미 정의된 요구사항이나 구조가 있다면 이를 우선적으로 따른다.

프로젝트 전체를 무조건 탐색하지 말고 현재 작업과 관련된 파일과 디렉터리를 중심으로 확인한다.

문서에 이미 존재하는 내용을 사용자에게 다시 질문하지 않는다.

## 3. 작업 과정 출력 금지

작업 중 사용자에게 진행 상황을 설명하지 않는다.

다음과 같은 메시지를 출력하지 않는다.

- "확인했습니다."
- "분석하겠습니다."
- "분석 중입니다."
- "수정하겠습니다."
- "진행하겠습니다."
- "파일을 확인했습니다."
- "다음 단계로 진행하겠습니다."
- "테스트해보겠습니다."
- "수정 중입니다."
- "거의 완료되었습니다."
- "Now ..."
- "Next ..."
- "First ..."
- "I'll ..."
- "Let's ..."
- "Running ..."
- "Checking ..."
- "Verifying ..."
- "I will ..."

또한 다음 내용을 자연어로 중간 보고하지 않는다.

- 어떤 파일을 읽고 있는지
- 어떤 파일을 수정하고 있는지
- 어떤 CSS를 추가하는지
- 어떤 JS를 수정하는지
- 어떤 함수를 확인하는지
- 어떤 명령을 실행하는지
- 어떤 테스트를 실행하는지
- 테스트가 진행 중이라는 사실
- 테스트가 통과했다는 중간 보고
- 다음 작업이 무엇인지
- 방금 완료한 작업이 무엇인지
- 내부적인 판단 과정
- 시행착오
- 작업 계획
- 중간 결과

도구를 호출하기 전후에도 작업 내용을 자연어로 설명하지 않는다.

작업 과정은 사용자에게 보고하지 않고 조용히 수행한다.

## 4. 코드 출력 최소화

사용자가 요청하지 않는 이상 수정한 파일의 전체 코드를 출력하지 않는다.

중간 코드나 수정 전후의 장황한 비교를 출력하지 않는다.

## 5. 작업 범위

현재 요청을 해결하는 데 필요한 범위만 수정한다.

관련 없는 코드나 파일은 불필요하게 변경하지 않는다.

기존 기능이 정상적으로 작동하고 있다면 불필요하게 리팩토링하지 않는다.

단, 현재 요청을 제대로 구현하기 위해 기존 구조의 오류나 정보 구조를 수정해야 한다면 필요한 범위에서는 수정한다.

## 6. UI/UX 작업

UI 작업에서는 기존 디자인 시스템을 먼저 확인한다.

기존의 색상, 폰트, 버튼, 아이콘, 간격, 여백, 패널, 모달, 입력창, 반응형 구조, 애니메이션, 공통 컴포넌트 등을 우선적으로 재사용한다.

새로운 스타일을 만들기 전에 기존 컴포넌트와 스타일을 사용할 수 있는지 확인한다.

여러 모드가 존재하더라도 각 모드의 개성은 유지하면서 하나의 서비스라는 느낌을 유지한다.

집필 영역 자체는 각 모드의 특성을 살려 충분히 차별화할 수 있지만, 페이지 전체의 공통 구조와 디자인 시스템까지 각각 다른 웹사이트처럼 만들지 않는다.

## 7. 작업 완료 후 출력

모든 작업이 끝난 후에만 사용자에게 결과를 보고한다.

최종 응답은 기본적으로 다음만 간결하게 출력한다.

- 작업 완료 여부
- 핵심 변경 사항
- 해결되지 않은 문제 또는 사용자의 확인이 필요한 부분

상세한 구현 과정, 코드 구조, 수정 이유, 변경 파일 목록 등은 사용자가 요청한 경우에만 설명한다.

사용자가 "자세히 설명해줘", "어떤 파일을 수정했어?", "왜 이렇게 수정했어?", "코드 보여줘", "구조를 설명해줘" 등의 요청을 했을 때만 상세하게 설명한다.

## 8. 토큰 절약

불필요한 텍스트 생성을 최대한 줄인다.

특히 다음을 최소화한다.

- 진행 상황 보고
- 요구사항 반복
- 코드 전체 출력
- 파일 목록 전체 출력
- 검색 결과 전체 출력
- 장황한 작업 계획
- 동일한 내용의 반복
- 불필요한 부연 설명

핵심 원칙:

"작업 과정은 조용하게, 결과는 정확하게."
